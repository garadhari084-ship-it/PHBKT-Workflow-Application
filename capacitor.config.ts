import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phbkt.workflow',
  appName: 'PHBKT Workflow',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
